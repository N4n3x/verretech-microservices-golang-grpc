version: '3.0'

services:
  produit:
    build:
      context: .
      dockerfile: /home/ubuntu/verretech-microservices/produit/install/Dockerfile
  utilisateur:
    build:
      context: .
      dockerfile: /home/ubuntu/verretech-microservices/utilisateur/install/Dockerfile
